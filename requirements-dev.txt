# Dev/test-only dependencies - not needed to run the bot itself.
# Install with: pip install -r requirements.txt -r requirements-dev.txt
# Run tests with: pytest
-r requirements.txt
pytest==9.1.1
pytest-asyncio==1.4.0
